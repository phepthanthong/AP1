#include <iostream>
#include <math.h>
#include <cstdlib>
using namespace std;
/*
int main()
{
  int j=10;
  int k=20;
  int *adr1 = &j;
  int *adr2;
  adr2=&k;
  *adr1=5;
  cout << *adr1 << '=' << j << endl;
  if (adr1==adr2)
    cerr << "Impossible! 2 variables diff ont des adresses diff " << endl;
  adr2 = adr1;
  if (adr1==adr2)
    cerr << "Ces 2 pointeurs pointent sur la meme adresse" << endl;
  if (*adr1==*adr2)
    cerr << "Oui, les contenus sont identiques" << endl;
  if (&adr1==&adr2)
    cerr << "Impossible! Ce sont 2 variables differents";
  adr2++;
  cout << "la variable j a pour adresse " << adr1 << endl
       << "la cade suivante a pour adresse " << adr2 << endl;
  if ((adr1+1) == adr2)
    cerr << "Oui,ces 2 pointeurs pointent sur la meme adresse " << endl;
}

int main()
{
  int tab[10];
  tab[0]=5; tab[1]=10;
  cout << tab[0] << endl;
  cout << *tab << endl;
  int *poin=tab;
  cout << *(poin+1) << endl;
  cout << poin[1] << endl;
}
*/
