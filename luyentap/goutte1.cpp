#include <iostream>
#include <math.h>
#include <cstdlib>

#define N 20
#define NBRE_GOUTTE 2

using namespace std;

char screen[N][N];

void paysage() {
  for (int i=0; i<n; i++)
    for (int j=0; j<n; j++)
      if (i==0 || i==n-1 ||j==0 || j==n-1)
		    cout << "#";
		  else if (a==j && b==i)
		    cout << "0";
		  else cout << " ";
type struct {
  double x;
  double y;
  double dx;
  double dy;
} Sprite;
void init_sprite(Sprite & s, double x, double y, double dx, double dy);
void mise_a_jour_sprite(
